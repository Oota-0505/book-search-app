fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
# ログインフォームの受け取りに必要（FastAPI の Form）
python-multipart==0.0.32
# Cloudflare Workers には生のTCPが無く fetch 経由になるため、requests ではなく httpx
httpx==0.28.1

# アイコン生成（tools/make_icons.py）とテストで使用
pillow==12.3.0
pytest==9.1.1
pytest-asyncio==1.4.0

# プッシュ通知
pywebpush==2.0.3
py-vapid==1.9.2